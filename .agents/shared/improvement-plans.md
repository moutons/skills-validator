# Improvement Plans

## From skill improvements

### General patterns to enforce in all skills

1. **Validation paths**: Use `skills-validator validate .agents/skills/<skill-name>/` (directory path, not just name)

2. **Always use directory path in validation commands**
