rules:
  unpinned-uses:
    config:
      policies:
        "dtolnay/rust-toolchain": ref-pin
  cache-poisoning: {}  # fixed: PR jobs use cache/restore (read-only), save only on push
  dangerous-triggers:
    ignore:
      - release-plz.yml